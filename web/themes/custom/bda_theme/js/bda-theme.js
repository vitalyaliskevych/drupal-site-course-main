/**
 * @file
 * BDA Theme behaviors.
 */
(function (Drupal) {

  'use strict';

  Drupal.behaviors.bdaTheme = {
    attach: function (context, settings) {

      console.log('It works!');

    }
  };

} (Drupal));
